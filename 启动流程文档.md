# AIOps Agent Platform - 部署启动手册

> **最后更新**: 2026-06-29
> **适用范围**: 本地开发环境 + Docker Compose 部署

---

## 项目概述

AIOps Agent Platform 是一个基于多智能体架构的智能化运维故障定位系统。通过 7 个专业 Agent（Monitor、RCA、Heal、Change、Memory、Eval、Orchestrator）协作，实现从 Prometheus 告警接收 → 异常检测 → 根因分析 → 自愈执行 → 变更审批的全流程自动化。

### 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| **后端框架** | FastAPI + Uvicorn (Python 3.11+) | 异步 HTTP + WebSocket |
| **Agent 编排** | 自研管道 (routes.py) + LangGraph 预留 | 4 步串行管道 |
| **LLM** | DeepSeek / OpenAI 可切换 | 通过 API Key 接入 |
| **向量数据库** | ChromaDB (嵌入式 PersistentClient) | 零配置，本地文件存储 |
| **嵌入模型** | all-MiniLM-L6-v2 (可选，384维) | 未安装时自动回退 hash embedding |
| **监控采集** | Prometheus + Node Exporter | 15s 抓取间隔 |
| **告警推送** | AlertManager → Webhook | 分组去重后推送到 Backend |
| **前端** | React 18 + TypeScript + Vite + Tailwind CSS | SPA |
| **部署** | Docker Compose (7 个服务) | 一键启动 |

---

## 〇、依赖组件总览

系统包含 **7 个运行时组件**，分为应用层和基础设施层：

```
┌─────────────────────────────────────────────────────────────────┐
│                      应用层 (Application)                        │
│  ┌──────────────────┐  ┌──────────────────┐                    │
│  │  Backend (:8000) │  │  Frontend (:3000)│                    │
│  │  FastAPI + Agent │  │  React + Vite    │                    │
│  └────────┬─────────┘  └──────────────────┘                    │
│           │                                                      │
│  ┌────────┴──────────────────────────────────┐                  │
│  │           ChromaDB (:8001)                 │                  │
│  │   向量数据库 — 记忆系统持久化                │                  │
│  │   嵌入模型: all-MiniLM-L6-v2 (可选)         │                  │
│  └───────────────────────────────────────────┘                  │
├─────────────────────────────────────────────────────────────────┤
│                    基础设施层 (Infrastructure)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐      │
│  │ Prometheus   │  │ AlertManager │  │  Node Exporter   │      │
│  │ (:9090)      │  │ (:9093)      │  │  (:9100)         │      │
│  │ 指标采集+存储 │  │ 告警分组+路由│  │  宿主机指标暴露   │      │
│  │ 15天保留     │  │ → Webhook    │  │  CPU/内存/磁盘    │      │
│  └──────────────┘  └──────┬───────┘  └──────────────────┘      │
│                           │                                      │
│                     Webhook POST                                │
│              /api/v1/webhooks/alertmanager                       │
└─────────────────────────────────────────────────────────────────┘
```

| 组件 | 端口 | 类型 | 部署方式 | 必须 |
|------|------|------|---------|:---:|
| **Backend** | 8000 | 应用 | Docker / 本地 Python | ✓ |
| **Frontend** | 3000 | 应用 | Docker / 本地 Node | ✓ |
| **ChromaDB** | 8001 | 数据 | Docker / 嵌入式 | ○ |
| **Prometheus** | 9090 | 监控 | Docker | ○ |
| **AlertManager** | 9093 | 告警 | Docker | ○ |
| **Node Exporter** | 9100 | 采集 | Docker | ○ |
| **LLM API** | — | 外部 | DeepSeek / OpenAI 云 API | ✓ |

> **○ = 可选**：ChromaDB 未部署时记忆系统回退到内存存储；监控栈未部署时系统仍可手动触发故障处理。

---

## 一、环境要求

| 依赖 | 版本要求 | 检查命令 | 说明 |
|------|----------|----------|------|
| Python | >= 3.11 | `python3.11 --version` | 使用了 `X \| None` 联合类型语法 |
| Node.js | >= 20 | `node --version` | 前端构建 |
| npm | >= 10 | `npm --version` | 前端包管理 |
| Docker | >= 24 | `docker --version` | 容器运行时 |
| Docker Compose | >= 2 | `docker compose version` | 多容器编排 |

---

## 二、项目结构

```
aiops-agent-platform/
├── backend/                        # FastAPI 后端
│   ├── app/
│   │   ├── main.py                 # FastAPI 入口 + /metrics + /health
│   │   ├── config.py               # 配置管理 (含 MemoryConfig)
│   │   ├── api/
│   │   │   ├── routes.py           # RESTful API + 管道编排 (含记忆集成)
│   │   │   ├── webhooks.py         # AlertManager Webhook 接收 (含记忆集成)
│   │   │   └── websocket.py        # WebSocket 实时推送
│   │   ├── agents/                 # 7 个 Agent
│   │   │   ├── monitor_agent.py    # 3算法投票异常检测
│   │   │   ├── rca_agent.py        # 贝叶斯+BFS+RAG+历史记忆检索
│   │   │   ├── heal_agent.py       # Playbook 自愈+熔断
│   │   │   ├── change_agent.py     # 5因素风险评分+审批
│   │   │   ├── memory_agent.py     # 三层记忆管理
│   │   │   ├── eval_agent.py       # 四维评估
│   │   │   └── business_monitor_agent.py  # 业务规则检测
│   │   ├── memory/                 # 记忆系统
│   │   │   ├── core.py             # MemorySystem 统一入口 + MemoryEnhancer
│   │   │   ├── short_term.py       # 短期记忆 (滑动窗口, 30min TTL)
│   │   │   ├── long_term.py        # 长期记忆 (ChromaDB + RRF + 时间衰减)
│   │   │   ├── working_memory.py   # 工作记忆 (Incident 绑定, TTL 槽位)
│   │   │   └── storage.py          # 存储层 (InMemory + ChromaDB + hash embedding)
│   │   ├── evaluation/             # 评估框架
│   │   │   ├── core.py             # EvaluationFramework 单例
│   │   │   ├── end_to_end.py       # 端到端 (7指标)
│   │   │   ├── reasoning_eval.py   # 推理评估 (5指标 + Rubric + ECE)
│   │   │   ├── tool_call_eval.py   # 工具调用 (5指标 + DoVer)
│   │   │   └── rag_eval.py         # RAG评估 (6指标)
│   │   ├── models/                 # Pydantic 数据模型
│   │   ├── services/               # IncidentService
│   │   ├── tools/                  # 记忆工具 + 评估工具
│   │   ├── nlu/                    # NLU 管道 (意图→实体→指标映射)
│   │   └── data/                   # 静态数据 (知识库/Playbook/拓扑/数据集)
│   ├── tests/                      # 后端测试 (175+)
│   ├── requirements.txt
│   └── .env                        # LLM API Key 等配置
├── frontend/                       # React 前端
│   ├── src/
│   │   ├── App.tsx                 # 路由配置
│   │   ├── pages/                  # 6 个页面
│   │   └── hooks/                  # useApi + useWebSocket
│   └── vite.config.ts              # 开发代理配置
├── infrastructure/                 # 监控基础设施配置
│   ├── prometheus/
│   │   ├── prometheus.yml          # 抓取配置 (3个 job)
│   │   └── rules/
│   │       └── aiops-alerts.yml    # 8 条告警规则
│   └── alertmanager/
│       └── alertmanager.yml        # Webhook 路由到 Backend
├── docker-compose.yml              # 生产环境 (应用 + ChromaDB)
├── docker-compose.dev.yml          # 开发环境 (全部 7 个服务)
├── deploy.sh                       # 一键部署脚本
├── 启动流程文档.md                  # 本文档
└── 记忆管理与评测系统深度解析.md     # 系统架构深度分析
```

---

## 三、配置 LLM (必须)

### 3.1 编辑环境变量

文件路径: `backend/.env`

```bash
# ===================================================================
# LLM 配置 (必须 — 驱动 Agent 推理)
# ===================================================================
LLM_PROVIDER=deepseek              # deepseek | openai | azure-openai
LLM_API_KEY=sk-your-api-key-here   # 替换为真实 API Key
LLM_MODEL=deepseek-chat
LLM_TEMPERATURE=0.1
LLM_MAX_TOKENS=4096
LLM_TIMEOUT_SECONDS=60
LLM_MAX_RETRIES=3

# ===================================================================
# 应用配置
# ===================================================================
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000
APP_LOG_LEVEL=INFO
APP_DEBUG=true
APP_CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ===================================================================
# 记忆系统配置
# ===================================================================
MEMORY_SHORT_TERM_MAX_ITEMS=100        # STM 最大条目数
MEMORY_LONG_TERM_MAX_ITEMS=10000       # LTM 最大条目数
MEMORY_SIMILARITY_THRESHOLD=0.85       # 记忆去重相似度阈值
MEMORY_RETENTION_DAYS=90               # LTM 保留天数
CHROMA_DB_PATH=./data/chroma           # ChromaDB 数据目录 (嵌入式模式)
```

### 3.2 支持的 LLM 提供商

| 提供商 | `LLM_PROVIDER` | `LLM_MODEL` 示例 |
|--------|---------------|-----------------|
| DeepSeek | `deepseek` | `deepseek-chat` |
| OpenAI | `openai` | `gpt-4o`, `gpt-4o-mini` |
| Azure OpenAI | `azure-openai` | 按实例配置 |

---

## 四、方式 A：Docker Compose 一键部署 (推荐)

这是最完整的方式，同时启动全部 7 个服务（应用层 3 个 + 监控层 3 个 + 数据层 1 个）。

### 4.1 启动全部服务

```bash
# 1. 配置 LLM (必须)
cp backend/.env.example backend/.env
# 编辑 backend/.env，填入 LLM_API_KEY

# 2. 一键启动
./deploy.sh dev

# 或直接使用 docker compose:
docker compose -f docker-compose.dev.yml up -d
```

### 4.2 各服务访问地址

| 服务 | 地址 | 说明 | 验证方式 |
|------|------|------|---------|
| **Frontend** | http://localhost:3000 | React SPA | 浏览器打开 |
| **Backend API** | http://localhost:8000 | FastAPI | `curl /health` → `{"status":"healthy"}` |
| **API 文档** | http://localhost:8000/docs | Swagger UI | 交互式文档 |
| **ChromaDB** | http://localhost:8001 | 向量数据库 | `curl localhost:8001/api/v1/heartbeat` |
| **Prometheus** | http://localhost:9090 | 监控指标查询 | 浏览器打开 → Graph 查询 `up` |
| **AlertManager** | http://localhost:9093 | 告警管理 | 浏览器打开 → 查看 Alerts |
| **Node Exporter** | http://localhost:9100/metrics | 宿主机指标 | `curl localhost:9100/metrics` |

### 4.3 监控栈数据流

```
Node Exporter (:9100) ──→ Prometheus (:9090) ──→ AlertManager (:9093)
                                │                       │
                         采集 CPU/内存/磁盘      评估告警规则 (8条)
                         15s 抓取间隔            分组/去重/静默
                                                     │
                                                     │ Webhook POST
                                                     ▼
                              Backend (:8000) /api/v1/webhooks/alertmanager
                                                     │
                                                     ▼
                              Monitor → RCA → Heal → Change 管道
                                                     │
                                                     ▼
                              记忆系统 (STM/LTM/WM) 持久化
```

### 4.4 查看日志

```bash
# 所有服务
docker compose -f docker-compose.dev.yml logs -f

# 单个服务
docker compose -f docker-compose.dev.yml logs -f backend-dev
docker compose -f docker-compose.dev.yml logs -f prometheus
```

### 4.5 停止服务

```bash
# 停止并保留数据卷
docker compose -f docker-compose.dev.yml down

# 停止并删除数据卷
docker compose -f docker-compose.dev.yml down -v
```

---

## 五、方式 B：本地开发启动 (逐个组件)

### 5.1 安装 Python 依赖

```bash
cd backend

# 使用 Python 3.11+ 创建虚拟环境
python3.11 -m venv venv
source venv/bin/activate

# 安装核心依赖
pip install -r requirements.txt
```

**核心依赖清单**：

| 包名 | 版本 | 用途 |
|------|------|------|
| `fastapi` | >=0.115 | Web 框架 |
| `uvicorn` | >=0.32 | ASGI 服务器 |
| `chromadb` | >=0.4.24 | 向量数据库 (嵌入式) |
| `scikit-learn` | >=1.5 | Isolation Forest 异常检测 |
| `numpy` | >=1.26 | 数值计算 |
| `pydantic` | >=2.0 | 数据校验 |
| `sentence-transformers` | 可选 | 语义嵌入 (未安装时自动回退 hash) |

### 5.2 启动 ChromaDB (可选)

ChromaDB 支持两种模式：

**模式 1：嵌入式 (默认，零配置)**

Backend 启动时自动在进程内创建 `PersistentClient`，数据存于 `backend/data/chromadb/`。
无需额外操作。如果 `chromadb` 包未安装或初始化失败，自动回退到内存存储。

**模式 2：独立服务 (Docker，推荐生产环境)**

```bash
docker run -d \
  --name chromadb \
  -p 8001:8000 \
  -v chroma-data:/chroma/chroma \
  -e IS_PERSISTENT=TRUE \
  -e ANONYMIZED_TELEMETRY=FALSE \
  chromadb/chroma:latest

# 验证
curl http://localhost:8001/api/v1/heartbeat
# → {"nanosecond heartbeat": ...}
```

使用独立服务时，设置环境变量：
```bash
CHROMA_DB_HOST=localhost
CHROMA_DB_PORT=8001
```

### 5.3 启动 Prometheus + AlertManager + Node Exporter (可选)

如果不使用 Docker Compose 完整部署，可以单独启动监控组件：

```bash
# 1. Node Exporter — 宿主机指标
docker run -d --name node-exporter \
  --net=host \
  -v /proc:/host/proc:ro -v /sys:/host/sys:ro \
  prom/node-exporter:latest \
  --path.procfs=/host/proc --path.sysfs=/host/sys

# 2. Prometheus — 指标采集
docker run -d --name prometheus \
  -p 9090:9090 \
  -v $(pwd)/infrastructure/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro \
  -v $(pwd)/infrastructure/prometheus/rules:/etc/prometheus/rules:ro \
  prom/prometheus:latest

# 3. AlertManager — 告警路由
docker run -d --name alertmanager \
  -p 9093:9093 \
  -v $(pwd)/infrastructure/alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro \
  prom/alertmanager:latest
```

> **注意**：单独启动时，Prometheus 通过 `host.docker.internal:8000` 抓取宿主机 Backend 指标，需确保 Backend 已在宿主机运行。

### 5.4 启动后端

```bash
cd backend
source venv/bin/activate

# 开发模式 (热重载)
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 验证
curl http://localhost:8000/health
# → {"status":"healthy","service":"aiops-agent-platform","version":"0.1.0"}

curl http://localhost:8000/metrics
# → Prometheus 格式的指标 (aiops_app_info, aiops_agent_up, aiops_incidents_total...)
```

### 5.5 启动前端

```bash
cd frontend
npm install
npm run dev

# 验证
curl http://localhost:3000
# → HTML 页面
```

---

## 六、依赖组件配置详解

### 6.1 ChromaDB 配置

**嵌入式模式 (默认)** — `backend/app/memory/storage.py:489-530`

```python
# 自动使用 PersistentClient，数据存本地文件
self._client = chromadb.PersistentClient(
    path="./data/chromadb",
    settings=Settings(anonymized_telemetry=False, allow_reset=True),
)
```

**嵌入模型** — `backend/app/memory/storage.py:54-80`

```python
# 优先级:
# 1. sentence_transformers.SentenceTransformer("all-MiniLM-L6-v2") — 384维
# 2. hash_based_embedding() — SHA-256 确定性哈希，纯 Python，零依赖
```

**记忆配置** — `backend/app/config.py:99-121`

| 环境变量 | 默认值 | 说明 |
|---------|--------|------|
| `MEMORY_SHORT_TERM_MAX_ITEMS` | 100 | 短期记忆最大条目数 |
| `MEMORY_LONG_TERM_MAX_ITEMS` | 10000 | 长期记忆最大条目数 |
| `MEMORY_SIMILARITY_THRESHOLD` | 0.85 | 记忆去重/合并的余弦相似度阈值 |
| `MEMORY_RETENTION_DAYS` | 90 | LTM 保留天数（超过后遗忘，important≥0.8且access≥5的保护） |

### 6.2 Prometheus 配置

**抓取目标** — `infrastructure/prometheus/prometheus.yml`

| Job | 目标 | 间隔 | 说明 |
|-----|------|------|------|
| `prometheus` | localhost:9090 | 15s | 自身监控 |
| `aiops-backend` | host.docker.internal:8000 | 15s | Backend `/metrics` 端点 |
| `node-exporter` | node-exporter:9100 | 15s | 宿主机 CPU/内存/磁盘/网络 |

**告警规则** — `infrastructure/prometheus/rules/aiops-alerts.yml` (8 条)

| 告警名 | 条件 | 持续时间 | 严重级别 |
|--------|------|:---:|:---:|
| `HighCPUUsage` | CPU > 80% | 5m | warning |
| `CriticalCPUUsage` | CPU > 95% | 2m | critical |
| `HighMemoryUsage` | Memory > 85% | 5m | warning |
| `CriticalMemoryUsage` | Memory > 95% | 2m | critical |
| `HighDiskUsage` | Disk > 85% | 5m | warning |
| `NodeDown` | Node Exporter 失联 | 1m | critical |
| `BackendDown` | Backend 失联 | 1m | critical |
| `HighErrorRate` | 5xx > 5% | 5m | high |

### 6.3 AlertManager 配置

**路由** — `infrastructure/alertmanager/alertmanager.yml`

```yaml
route:
  receiver: "aiops-webhook"
  group_by: ["alertname", "severity"]   # 按告警名+严重级别分组
  group_wait: 10s                        # 首次等待（收集同组告警）
  group_interval: 10s                    # 同组后续告警间隔
  repeat_interval: 5m                    # 重复发送间隔

receivers:
  - name: "aiops-webhook"
    webhook_configs:
      - url: "http://host.docker.internal:8000/api/v1/webhooks/alertmanager"
        send_resolved: true              # 恢复通知也发送
```

**端到端流程**：

```
CPU 飙升至 96% → Prometheus 评估规则 → CriticalCPUUsage 触发
  → AlertManager 接收 → 分组去重 → POST webhook
  → Backend webhooks.py 接收 → 创建 Incident
  → MonitorAgent (异常检测) → RCAAgent (根因分析)
  → HealAgent (自愈方案) → ChangeAgent (审批)
  → 每步结果自动存入 MemorySystem (STM→LTM)
```

### 6.4 记忆系统管道集成

**已接入**：管道的每个 Agent 步骤完成后自动调用 `MemorySystem.store()`：

```
Monitor 完成后 → store(OBSERVATION, importance=0.9, tags=["monitor","anomaly_detection",...])
RCA 完成后    → store(EPISODIC, importance=0.85, tags=["rca","root_cause",...])
                  + RCA 执行前 search() 历史相似故障 → 置信度加成
Heal 完成后   → store(PROCEDURAL, importance=0.7, tags=["heal","playbook",...])
Change 完成后 → store(EPISODIC, importance=0.6, tags=["change","approval",...])
RESOLVED 后   → flow_wm_to_ltm()  — 工作记忆归档到长期记忆
```

初始化失败（如 ChromaDB 不可用）时静默跳过，不影响管道正常执行。

---

## 七、API 端点速查

### 故障管理

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/api/v1/incidents/trigger` | 手动触发故障处理 |
| GET | `/api/v1/incidents/{id}` | 查询故障详情 (含 RCA/Heal/Change 结果) |
| GET | `/api/v1/incidents` | 故障列表 (分页+过滤) |
| POST | `/api/v1/incidents/trigger-business` | 业务异常检测 |
| POST | `/api/v1/incidents/diagnose` | 自然语言诊断 (大白话→Agent管道) |

### Webhook

| 方法 | 端点 | 说明 |
|------|------|------|
| POST | `/api/v1/webhooks/alertmanager` | AlertManager 告警接收 |
| GET | `/api/v1/webhooks/alertmanager/health` | Webhook 健康检查 |

### Agent / 拓扑 / 评估 / 记忆

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/v1/agents` | Agent 列表 (7个) |
| GET | `/api/v1/agents/{id}/status` | Agent 状态 |
| GET | `/api/v1/topology` | 服务拓扑图 |
| GET | `/api/v1/evaluations` | 评估结果列表 |
| POST | `/api/v1/evaluations/run` | 触发评估任务 |
| GET | `/api/v1/memory/search` | 记忆搜索 |
| POST | `/api/v1/memory/store` | 存储记忆 |

### WebSocket

| 端点 | 说明 |
|------|------|
| `/ws` | 通用 WebSocket (频道订阅) |
| `/ws/incidents/{id}` | 故障实时状态推送 |

### 运维端点

| 端点 | 说明 |
|------|------|
| `/health` | 健康检查 |
| `/ready` | 就绪探针 (含 DB/Agent 状态) |
| `/metrics` | Prometheus 指标暴露 (aiops_app_info, aiops_agent_up, http_requests_total...) |

---

## 八、前端页面路由

| 路径 | 页面 | 说明 |
|------|------|------|
| `/` | Dashboard | 仪表盘/概览 |
| `/incidents` | IncidentsPage | 故障列表 |
| `/incidents/:id` | IncidentDetailPage | 故障详情 (含管道进度) |
| `/agents` | AgentsPage | 7个 Agent 管理 |
| `/evaluation` | EvaluationPage | 评估结果 (四维) |
| `/topology` | TopologyPage | 服务拓扑图 |
| `/memory` | MemoryPage | 记忆搜索 |

---

## 九、运行测试

### 后端测试

```bash
cd backend
source venv/bin/activate

# 使用 venv 的 Python 3.11+ (Python 3.9 会因 X|None 语法报错)
./venv/bin/python -m pytest tests/ -v

# 特定模块
./venv/bin/python -m pytest tests/test_memory_system.py -v   # 记忆系统 20 个
./venv/bin/python -m pytest tests/test_evaluation.py -v       # 评估系统 20 个
./venv/bin/python -m pytest app/tests/test_agents.py -v       # Agent 64 个
```

### 前端检查

```bash
cd frontend
npx tsc --noEmit          # TypeScript 类型检查
npm run build             # 生产构建
```

---

## 十、前后端交互架构

```
┌──────────────────────────────────────────────────────────────────┐
│                      浏览器 (Browser)                             │
│  http://localhost:3000                                            │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                    React SPA (Vite)                         │  │
│  │  ┌──────────┐  ┌──────────┐  ┌─────────────────┐          │  │
│  │  │ useApi() │  │useWebSocket│ │ Zustand Store   │          │  │
│  │  └────┬─────┘  └────┬─────┘  └─────────────────┘          │  │
│  └───────┼──────────────┼─────────────────────────────────────┘  │
│          │              │                                         │
│    /api/* (HTTP)   /ws/* (WebSocket)                              │
│          │              │                                         │
└──────────┼──────────────┼─────────────────────────────────────────┘
           │              │
    Vite Proxy      Vite Proxy
    (:3000→:8000)   (:3000→:8000)
           │              │
┌──────────┼──────────────┼─────────────────────────────────────────┐
│          ▼              ▼                                         │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │                 FastAPI Backend (:8000)                    │    │
│  │  ┌────────────┐  ┌──────────┐  ┌────────────────────┐    │    │
│  │  │ REST API   │  │WebSocket │  │ /metrics (Prom格式) │    │    │
│  │  │ + Webhook  │  │ Manager  │  │                    │    │    │
│  │  └─────┬──────┘  └────┬─────┘  └────────────────────┘    │    │
│  │        │               │                                   │    │
│  │        ▼               ▼                                   │    │
│  │  ┌──────────────────────────────────────────────────┐     │    │
│  │  │            Agent Pipeline (routes.py)             │     │    │
│  │  │  Monitor ──→ RCA ──→ Heal ──→ Change             │     │    │
│  │  │     │          │        │         │               │     │    │
│  │  │     └──────────┴────────┴─────────┘               │     │    │
│  │  │                    │                              │     │    │
│  │  │            MemorySystem                           │     │    │
│  │  │       STM ──→ LTM ←── WM                          │     │    │
│  │  └──────────────────────────────────────────────────┘     │    │
│  └──────────────────────────────────────────────────────────┘    │
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐   │
│  │ ChromaDB     │  │ DeepSeek/    │  │ Prometheus           │   │
│  │ (嵌入式/远程) │  │ OpenAI API   │  │ ← [:9090] 抓取指标   │   │
│  └──────────────┘  └──────────────┘  └──────────────────────┘   │
└──────────────────────────────────────────────────────────────────┘
```

---

## 十一、验证清单

### 必须验证 (核心功能)

- [ ] `backend/.env` 中 `LLM_API_KEY` 已填写
- [ ] Python 3.11+ venv 创建成功，`pip install -r requirements.txt` 无报错
- [ ] 后端启动: `curl http://localhost:8000/health` → `{"status":"healthy"}`
- [ ] API 文档可访问: http://localhost:8000/docs
- [ ] Agent 列表: `curl http://localhost:8000/api/v1/agents` → 7 个 Agent
- [ ] 故障触发: `curl -X POST http://localhost:8000/api/v1/incidents/trigger` → 202
- [ ] 前端启动: `http://localhost:3000` 可访问

### 可选验证 (记忆系统)

- [ ] ChromaDB 就绪: `curl http://localhost:8001/api/v1/heartbeat` (Docker 模式)
- [ ] 记忆存储: `curl -X POST http://localhost:8000/api/v1/memory/store` → 201
- [ ] 记忆搜索: `curl "http://localhost:8000/api/v1/memory/search?query=CPU"` → 返回结果

### 可选验证 (监控栈)

- [ ] Prometheus 就绪: http://localhost:9090 → 查询 `up` 检查所有 target
- [ ] Node Exporter: `curl http://localhost:9100/metrics` → CPU/内存指标
- [ ] AlertManager: http://localhost:9093 → 查看告警页面
- [ ] 告警规则加载: Prometheus → Status → Rules → 8 条规则
- [ ] Backend `/metrics`: `curl http://localhost:8000/metrics` → Prom 格式数据

### 可选验证 (端到端)

- [ ] 手动触发故障: `POST /api/v1/incidents/trigger` → 管道正常执行 → RESOLVED
- [ ] WebSocket 推送: 连接 `/ws` → 收到 incident_update 事件
- [ ] 记忆持久化: 管道完成后 → `/api/v1/memory/search?query=...` 能找到本次记忆
- [ ] 监控栈端到端: 构造高 CPU → Prometheus 触发告警 → AlertManager webhook → Backend 创建 Incident → 管道处理

---

## 十二、常见问题

### Q1: 启动后端报 `Address already in use`
```bash
lsof -ti :8000 | xargs kill -9
```

### Q2: Python 版本不兼容
```bash
# macOS 安装 Python 3.11
brew install python@3.11
# 使用明确路径创建 venv
python3.11 -m venv venv
```

### Q3: ChromaDB 初始化失败，管道能否正常运行？
**可以**。MemorySystem 初始化失败时，管道静默跳过所有记忆操作，Agent 正常执行。记忆系统回退到纯内存存储（`InMemoryStorage`），进程重启后数据丢失。

### Q4: 不装 `sentence-transformers`，嵌入怎么办？
自动回退到 `hash_based_embedding`（SHA-256 确定性哈希 + L2 归一化），纯 Python 实现，零外部依赖。相同文本总是产生相同向量，适用于去重和关键词匹配场景。语义搜索效果不如真实嵌入模型，但功能完整。

### Q5: 如何只启动核心服务（不启动监控栈）？
```bash
# 仅启动 Backend + Frontend + ChromaDB
docker compose -f docker-compose.dev.yml up -d backend-dev frontend-dev chroma-dev
```

### Q6: Prometheus 抓取 Backend 指标失败 (context deadline exceeded)
检查 Docker 网络：Prometheus 容器内访问宿主机的 Backend 需要使用 `host.docker.internal`。已在 `docker-compose.dev.yml` 中配置 `extra_hosts`。

### Q7: AlertManager Webhook 发送失败
1. 确认 Backend 已启动: `curl http://localhost:8000/health`
2. 确认 AlertManager 配置中的 URL 使用了 `host.docker.internal`
3. 查看 AlertManager 日志: `docker logs aiops-alertmanager`

### Q8: 如何查看记忆系统中存储了什么？
```bash
# 通过 API 搜索
curl "http://localhost:8000/api/v1/memory/search?query=order-service&top_k=10"

# 或直接查看 ChromaDB 数据 (嵌入式模式)
ls -la backend/data/chromadb/
```
